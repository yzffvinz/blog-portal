const URL_MAP = {
    'index': '',
    'detail': ''
};
function processRequest(request, response) {
    const url = request.url;
    url.match(/\/(?.*)[\/$]/);

}

module.exports = exports = processRequest;